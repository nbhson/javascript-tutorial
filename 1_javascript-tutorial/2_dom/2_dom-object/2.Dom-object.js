// Khi trang web được chạy nó sẽ tạo thành một Dom object, được lưu trong doccument (hình dạng cây) là đại diện cho toàn bộ website
// biến doccment này được tích hợp sẵn trong web API của JS
// nên muốn truy cập vào các thành phần của DOM HTML thì đều phải thông qua biến doccument này

console.log(document);

document.write('Hello Guys')