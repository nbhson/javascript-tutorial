// attribute: lấy ra tất cả attr của element

// autocapitalize: true thì element có thể chỉnh sửa như 1 input 
// (thêm attr contenteditable vào element)

// childNodeElement: đếm số lượng con là element bên trong

// childNodes: bao gồm cả textNode và Element note bên trong
// children: trả về element bên trong

// classList: dùng rất nhiều

// firstChild: trả về cái node đầu tiên bên trong (text node hoặc element node)

// firstElement: trả về thằng element đầu tiên thôi

// nextElementSibling: trả về thằng element tiếp theo (cùng cấp)

// nextSibling: trả về thằng element node hoặc text node tiếp theo (cùng cấp)

// nodeType: Element = 1, Attr = 2, Text = 3

// spell check: kiểm tra chính tả

// translate: ????